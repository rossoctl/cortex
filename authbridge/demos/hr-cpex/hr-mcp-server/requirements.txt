# fastapi 0.136.3 requires starlette>=0.46.0, which resolves to a
# release past the HIGH-severity starlette advisories that 0.41.x carried
# (multipart large-file DoS 0.47.2, Range/FileResponse DoS 0.49.1,
# Host header validation 1.0.1). Bumping uvicorn alone would not move
# the transitive starlette pin.
fastapi==0.136.3
uvicorn[standard]==0.32.0
